# ai-codegen

## Pre-requisites
- Java must be installed on your device and must be added to PATH

## Client APP
- Run as a typical react project
- `yarn install`
- `yarn start`

## Server APP
- Install dependencies by running `yarn install`
- Run dev mode via `yarn dev`
- Or Production mode via `yarn start`